% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/probit.R
\name{estimate_categ_func_data_probit}
\alias{estimate_categ_func_data_probit}
\title{Estimate Z and p curves using (generalized) probit or binomial models}
\usage{
estimate_categ_func_data_probit(
  time_points,
  x_array,
  n_basis = 25,
  method = "ML",
  threshold_probability = 0.004
)
}
\arguments{
\item{time_points}{A numeric vector of time values (length T)}

\item{x_array}{A 3D array (individual × time × category) of one-hot encoded data}

\item{n_basis}{Number of basis functions for smoothing (default = 25)}

\item{method}{GAM fitting method (default = "ML")}

\item{threshold_probability}{Probability threshold for switching to probit special case (default = 0.004)}
}
\value{
A list containing estimated Z curves and category probabilities for each individual
}
\description{
Estimates latent Gaussian processes using adaptive probit/binomial switching.
Uses probit models for rare events (sparse categories) and binomial models
for common events.
}
\details{
This function implements an adaptive approach that switches between probit
and binomial link functions based on the sparsity of categorical events:
\itemize{
\item Uses probit link for rare events (proportion < threshold_probability)
\item Uses binomial link for common events
\item Only applies special probit handling for short time series (≤ 301 points)
}

The adaptive approach helps with numerical stability when dealing with
very rare categorical events.
}
\examples{
# Generate sample one-hot encoded data
set.seed(123)
n_time <- 50
n_individuals <- 8
n_categories <- 3

time_points <- seq(0, 1, length.out = n_time)
x_array <- array(0, dim = c(n_individuals, n_time, n_categories))

# Fill with random one-hot vectors
for(i in 1:n_individuals) {
  for(t in 1:n_time) {
    cat <- sample(1:n_categories, 1)
    x_array[i, t, cat] <- 1
  }
}

# Estimate using probit approach
result <- estimate_categ_func_data_probit(time_points, x_array)
names(result)

}
